# 프로젝트 구조 및 설정 파일 가이드

## 📁 전체 프로젝트 구조

```
webadb-auto-installer/
│
├── server.js                    # Express 백엔드 서버
├── package.json                 # 프로젝트 의존성 및 설정
├── package-lock.json            # 의존성 잠금 파일 (npm install 시 자동 생성)
│
├── apk-config.json              # 현재 배포 중인 APK 정보 (서버가 자동 생성/관리)
│
├── apks/                        # APK 파일 저장소 (서버가 자동 생성)
│   ├── app-1706078400000.apk   # 업로드된 APK 파일들
│   └── app-1706164800000.apk
│
├── public/                      # 정적 파일 (웹 페이지)
│   ├── index.html              # 사용자 메인 페이지
│   ├── admin.html              # 관리자 APK 관리 페이지
│   ├── app.js                  # 기본 WebADB 클라이언트
│   └── app-advanced.js         # 고급 WebADB 클라이언트 (권장)
│
├── node_modules/                # npm 패키지들 (npm install 시 자동 생성)
│
├── README.md                    # 프로젝트 설명 및 사용 가이드
└── ANDROID_SETUP_GUIDE.md      # 안드로이드 USB 디버깅 설정 가이드
```

## 📝 설정 파일 상세 설명

### 1. apk-config.json

서버가 자동으로 생성하고 관리하는 파일입니다. 현재 배포 중인 APK 정보를 저장합니다.

**파일 위치**: 프로젝트 루트 디렉토리

**내용 예시**:
```json
{
  "filename": "app-1706078400000.apk",
  "originalName": "MyTestApp_v1.0.0.apk",
  "uploadDate": "2024-01-24T12:00:00.000Z",
  "size": 15728640
}
```

**필드 설명**:
- `filename`: 서버에 저장된 실제 파일명 (타임스탬프 포함)
- `originalName`: 업로드 시 원본 파일명
- `uploadDate`: 업로드 날짜/시간 (ISO 8601 형식)
- `size`: 파일 크기 (바이트 단위, 예시는 15MB)

**수동 생성 시기**: 
- 처음 서버 실행 시 자동으로 빈 설정으로 생성됩니다
- APK를 업로드하면 자동으로 업데이트됩니다
- 수동으로 생성할 필요는 없습니다

### 2. apks/ 폴더

APK 파일들이 저장되는 디렉토리입니다.

**폴더 위치**: 프로젝트 루트 디렉토리

**자동 생성**: 
- 서버 첫 실행 시 자동으로 생성됩니다
- 없으면 `mkdir -p apks` 코드가 자동 실행됩니다

**파일명 규칙**:
```
app-[타임스탬프].apk

예시:
app-1706078400000.apk   # 2024-01-24 12:00:00
app-1706164800000.apk   # 2024-01-25 12:00:00
```

**파일 관리**:
- 새 APK 업로드 시 이전 APK는 자동 삭제됩니다
- 한 번에 하나의 APK만 배포됩니다
- 관리자 페이지에서 삭제 가능합니다

## 🚀 초기 설정 단계별 가이드

### Step 1: 프로젝트 폴더 생성

```bash
mkdir webadb-auto-installer
cd webadb-auto-installer
```

### Step 2: 파일 배치

```bash
# 메인 파일들 (루트 디렉토리)
- server.js
- package.json
- README.md
- ANDROID_SETUP_GUIDE.md

# public 폴더 생성 및 파일 배치
mkdir public
cd public

# public 폴더 안에 배치
- index.html
- admin.html
- app.js
- app-advanced.js
```

### Step 3: 의존성 설치

```bash
npm install
```

이 명령으로 자동 생성되는 것들:
- `node_modules/` 폴더
- `package-lock.json` 파일

### Step 4: 서버 실행

```bash
npm start
```

서버가 자동으로 생성하는 것들:
- `apks/` 폴더
- `apk-config.json` 파일 (APK 없는 초기 상태)

## 📊 apk-config.json 상태별 예시

### 초기 상태 (APK 없음)

```json
{
  "filename": null,
  "originalName": null,
  "uploadDate": null,
  "size": 0
}
```

### APK 업로드 후

```json
{
  "filename": "app-1706078400000.apk",
  "originalName": "MyTestApp_v1.0.0.apk",
  "uploadDate": "2024-01-24T12:00:00.000Z",
  "size": 15728640
}
```

### 실제 큰 APK 예시

```json
{
  "filename": "app-1706078400000.apk",
  "originalName": "MyGame_v2.5.3_release.apk",
  "uploadDate": "2024-01-24T12:00:00.000Z",
  "size": 157286400
}
```
(157286400 바이트 = 약 150MB)

## 🔧 수동 설정이 필요한 경우

### APK를 수동으로 추가하는 방법

1. **APK 파일을 apks/ 폴더에 복사**
```bash
cp /path/to/your/app.apk apks/app-1706078400000.apk
```

2. **apk-config.json 수동 생성**
```bash
cat > apk-config.json << 'EOF'
{
  "filename": "app-1706078400000.apk",
  "originalName": "your-app.apk",
  "uploadDate": "2024-01-24T12:00:00.000Z",
  "size": 15728640
}
EOF
```

3. **파일 크기 확인** (Linux/Mac)
```bash
ls -l apks/app-1706078400000.apk
# 또는
stat -f%z apks/app-1706078400000.apk  # Mac
stat -c%s apks/app-1706078400000.apk  # Linux
```

## 📦 백업 및 복원

### 백업
```bash
# APK 및 설정 백업
tar -czf webadb-backup.tar.gz apks/ apk-config.json
```

### 복원
```bash
# 백업 복원
tar -xzf webadb-backup.tar.gz
```

## ⚠️ 주의사항

1. **apk-config.json은 서버가 관리합니다**
   - 수동 수정 시 서버 재시작 필요
   - JSON 형식 오류 시 서버 에러 발생

2. **apks/ 폴더의 파일명은 apk-config.json과 일치해야 합니다**
   - filename 필드와 실제 파일명이 다르면 다운로드 실패

3. **파일 크기 제한**
   - 현재 설정: 200MB (server.js에서 수정 가능)
   - 더 큰 파일이 필요하면 multer limits 수정

4. **권한 문제**
   - apks/ 폴더와 apk-config.json에 쓰기 권한 필요
   - Linux: `chmod 755 apks/`

## 🔍 문제 해결

### apk-config.json 오류
```bash
# 설정 파일 재생성
rm apk-config.json
npm start
# 서버가 자동으로 빈 설정 생성
```

### apks/ 폴더 누락
```bash
# 폴더 수동 생성
mkdir -p apks
chmod 755 apks
```

### 파일 동기화 문제
```bash
# apks/ 폴더 내용 확인
ls -lh apks/

# apk-config.json 내용 확인
cat apk-config.json

# 불일치 시 관리자 페이지에서 재업로드 권장
```

## 📱 테스트 APK 준비

실제 테스트를 위해서는:

1. **자체 개발한 APK 사용**
   - Android Studio로 빌드한 APK

2. **테스트용 샘플 APK**
   - 간단한 "Hello World" 앱 빌드
   - APK 크기: 보통 5-10MB

3. **기존 APK 파일**
   - 이미 가지고 있는 APK 파일 사용
   - 반드시 합법적으로 배포 가능한 APK여야 함

## 🎯 권장 워크플로우

1. 서버 실행 → 자동으로 apks/ 폴더와 apk-config.json 생성
2. 관리자 페이지에서 APK 업로드 → 자동으로 설정 업데이트
3. 사용자가 서비스 접속 → APK 자동 설치

**수동 파일 관리는 권장하지 않습니다!**
